# game-library ui
User interface for game-library app. 

### Usage with `Make`:
    build          builds app for production to the `build` folder
    run            runs app
    test           runs tests